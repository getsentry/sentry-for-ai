needs:
  - search-query-language.md
  # The whole concepts web. Debugging only reads a few of these — the error and
  # its attached context (logs/replay/profile/feedback), the trace, and the
  # monitor types that also create issues (cron missed/failed check-ins, metric
  # thresholds) plus the Monitors->Issues->Alerts model. The rest ship because
  # those docs cross-link them, and a pointer that resolves in the source repo
  # has to resolve in the built skill too. SKILL.md decides what gets read.
  - concepts/*.md
