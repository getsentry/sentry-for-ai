# Markdown flowmark must not touch. Gitignore syntax, but during a directory
# walk flowmark matches file patterns against the basename only, so single
# files are listed by name rather than by path.

# Generated by scripts/build-skill-tree.sh from a heredoc plus one row per
# skill. Formatting it would make the generator's output permanently "stale".
SKILL_TREE.md

# Frozen per-SDK wizard skills, excluded from the plugin build.
skills-legacy/
